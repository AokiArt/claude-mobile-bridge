# 全局约定

## 审批卡任务短标签（手机离开模式审批用）
当你在执行一个多步骤开发/操作任务时，每轮回答的第一行输出一个短标签：
`【任务：<≤15字的一句话，像待办清单项，例如"给状态条加变色系统"】`
电脑侧审批钩子会读取这个标签，让手机上的审批卡一眼看清"这是哪个任务在请求操作"。
- 只在动手干活的会话里需要；纯闲聊/简单问答不必强求
- 若某轮没有标签，审批卡会自动从用户原话提炼首句兜底
